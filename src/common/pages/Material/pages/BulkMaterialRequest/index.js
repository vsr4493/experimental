import React from 'react';
import TableForm from './TableForm';

const Page = (props) => {
    return (
        <div>
            <TableForm {...props} />
        </div>
    );
}

export default Page;